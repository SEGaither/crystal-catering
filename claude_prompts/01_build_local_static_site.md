# Claude Code Prompt — Build Local Static Site

You are Claude Code operating in a local terminal/TUI environment.

Task: Build the static landing page implementation for Crystal’s Beverage Catering Service from this local repo package.

Read `README.md`, `repo_manifest.json`, `planning/`, `documentation/`, and `claude_prompts/` before writing implementation files.

Build inside `website/`.

Requirements:
1. Create `index.html`, `styles.css`, `script.js`, `robots.txt`, `sitemap.xml`, `site.webmanifest`, and `data/site-config.json` if missing.
2. Use `assets/owner-crystal-bruce.jpeg` in the Meet Crystal section.
3. Build an upscale, mobile-first landing page.
4. Include quote CTA, date-check CTA, phone CTA, and email.
5. Build static FAQ chatbot with approved answers only.
6. Include SEO metadata, Open Graph, conservative JSON-LD, robots, sitemap.
7. Include quote form with honeypot and validation.
8. Do not hardcode API keys.
9. Do not invent unsupported claims.
10. Update documentation after build.

Return files created, how to preview, provisional items, and GitHub/Vercel readiness.
